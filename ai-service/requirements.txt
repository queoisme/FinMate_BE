# Runtime — chỉ những gì tiến trình phục vụ cần. Dockerfile chỉ cài file này.
# Thư viện training (torch/transformers/underthesea) nằm ở requirements-training.txt:
# máy phục vụ không cần chúng, và cài vào làm image phồng thêm ~2.5GB.
# Cả hai file cộng lại vẫn đúng bằng danh sách đã duyệt ở TECH_STACK.md §2.2.

# Web framework
fastapi==0.115.*
uvicorn[standard]==0.32.*

# Database
sqlalchemy==2.0.*
asyncpg==0.30.*
alembic==1.14.*

# Pydantic
pydantic==2.10.*
pydantic-settings==2.*

# ML — inference. scikit-learn kéo theo numpy/scipy/joblib.
scikit-learn==1.6.*
regex==2024.*

# OCR hóa đơn (Phase 10). pytesseract chỉ là lớp bọc mỏng gọi binary `tesseract` —
# binary và gói ngôn ngữ tiếng Việt cài ở tầng hệ điều hành trong Dockerfile.
# Chọn Tesseract thay vì EasyOCR/PaddleOCR vì hai thứ đó kéo `torch` ngược vào image
# PHỤC VỤ (~2.5GB), xoá đúng cái việc tách requirements-training.txt đã làm ở Phase 9.
pytesseract==0.3.*
Pillow==11.*

# HTTP client
httpx==0.28.*

# Caching
redis==5.*

# Utilities
python-multipart==0.0.*
python-jose[cryptography]==3.*
structlog==24.*

# Testing
pytest==8.*
pytest-asyncio==0.24.*
pytest-cov==6.*
